/** Remotion CLI entry — use `remotion/index.ts`, not `Root.tsx` (that file only defines compositions). */
import { registerRoot } from "remotion";
import { RemotionRoot } from "./Root";

registerRoot(RemotionRoot);
